# norm-boundary — 현재 상태와 진단 (읽기용)

*2026-07-24 정리. 실험이 어떻게 진행됐는지(연대기)가 아니라, **무엇을 어떻게 실험했고 어떤
결과가 나왔으며 지금 무엇이 단단하고 무엇이 아닌지**를 담는다. 모든 수치는 이번 정리 패스에서
동결 CSV로부터 직접 재검증했다. 완전한 기술 원장은 `results/RESULTS.md`, 이 파일은 그 읽기용
요약.*

---

## 0. 한 줄 요약

딥 시계열 예측기의 사실상 표준 계층인 **내생 instance normalization(RevIN 계열)에는 적용
경계가 있다.** 시계열의 레벨이 외생 공변량(풍력의 기상, 부하의 기온)으로 예측 가능할 때, 창
평균을 빼는 순간 복구 불가능한 신호가 파괴된다. 학습 전에 계산하는 스칼라 하나(**LPS**)가 어느
쪽인지를 알려준다. CondNorm은 이 경계를 드러내는 **도구**이지 우리가 미는 방법이 아니다.

---

## 1. 무엇을 실험했나 — 실험을 "무엇을 검증하는가"로 정리

연대순(A→F 블록)이 아니라 **증거의 강도 순**으로 정리한다. 이게 논문이 서야 할 순서이기도 하다.

| # | 실험 | 무엇을 검증 | 셋업 | 규모 |
|---|---|---|---|---|
| 1 | **구조적 결손 (Prop 1)** | IN이 함수 클래스에서 무엇을 제거하는가 | 대수 유도 + 실제 스택 float64 검증 + MC | — |
| 2 | **정보 대등 (Block B)** | 계층이 문제인가 정보가 문제인가 | 전 arm 동일 공변량, 5 백본, 외생 4 | 1,133 |
| 3 | **주 격자 (Block A)** | 경계가 데이터셋별로 부호를 뒤집는가 | 5 norm × 4 백본 × h × 5 seed | 1,794 |
| 4 | **분산 귀속 (G5)** | 경계가 어디에 있는가 | log-MSE 요인 분해 | (A에서) |
| 5 | **LPS 진단 (G4 사전등록)** | 학습 전 스칼라가 경계를 예측하는가 | w=96 LightGBM OOS R², τ=0.3 | 8 데이터셋 |
| 6 | **합성 검증 (G2, GATE 1)** | 이론 교차점이 실측과 맞는가 | λ×h×L×seed×norm | 2,640 |
| 7 | **SOTA 재현 (Block C/D)** | 패턴이 어댑터 아티팩트인가 | TimeXer/iTransformer에 arm 주입 | 1,275 |
| 8 | **Shrinkage 반증 (G8)** | "축소하면 진단 불필요"가 맞는가 | α 재보정 재학습, 사전등록 | 204 |
| 9 | **확률 예측 (Block F)** | 경계가 구간에도 전이되는가 | 분위수 arm, pinball+커버리지 | 644 |

**데이터:** 외생 {Jeju Wind(자가 큐레이션, 리드 매칭 KMA NWP), GEFCom-Wind/Load/Solar} +
표준 {ETTh1, ETTh2, Electricity, Weather}. 총 **4,202 run** + 확장.

---

## 2. 어떤 결과가 나왔나 — 단단한 것부터

### 2.1 IN은 창 레벨을 복구 불가능하게 제거한다 (Prop 1) — 단단함

합성 forecast는 **임의의 백본 f에 대해** `ŷ = ȳ + s·f((x−ȳ·1)/s, g)` 형태다. 즉 창 평균에
대해 **가법 분리 + 기울기 1**. 실제 코드를 float64로 구동해 확인: `|f(x+c)−f(x)−c| ≤ 2.7e-12`
(c=−5000). 따라서 IN은 창 레벨을 기울기 1로만 쓸 수 있고, 창 레벨×공변량 상호작용을 표현
불가능하다. Raw는 둘 다 가능하다.

**결정적 함의 — 유연성이 안 고친다:** 백본이 무제약이면 Raw는 Bayes에 도달(초과 0)하지만 IN은
양의 바닥 `[(a−1)² + κ²Var(g)]·Var(ȳ)(1−ϱ²) > 0`에 갇힌다. **IN−Raw 격차는 용량이 커질수록
줄지 않고 커진다.** MLP 데모가 확인(a=1에서 RevIN 초과 0.085 vs Raw 0.003).

### 2.2 계층이 문제이지 정보가 아니다 (Block B) — 단단함

모든 arm에 같은 공변량을 주고 계층만 토글한 순수 비용 `MSE(RevIN+cov) − MSE(Raw+cov)`:

| 백본 | 평균 | 95% CI | 셀>0 |
|---|---|---|---|
| LGBM-Cov | +0.0191 | [+0.0116, +0.0269] | 11/11 |
| Linear mixer | +0.0205 | [+0.0056, +0.0368] | 8/11 |
| MLP mixer | +0.0238 | [+0.0119, +0.0390] | 10/11 |
| SegRNN-Cov | +0.0112 | [+0.0058, +0.0172] | 10/11 |
| **PatchTST-Cov** | **+0.0038** | **[−0.0091, +0.0167]** | 7/11 |

5중 4개가 0을 배제, **PatchTST-Cov는 귀무**(그대로 보고). 크기는 하찮다(0.004~0.024) — 이건
계층 비용만 격리한 값이고, 공변량 접근 격차(+0.841)와 자릿수가 다른 게 정직한 그림이다.

### 2.3 전략 이중해리 (Table 1, 벤치마크+MASE) — 단단함

공변량 조건화 레벨 모델(1단계 회귀 단독 / ridge dynreg / CondNorm)이 최적 IN arm을 이기는가:

| 그룹 | MSE | MASE |
|---|---|---|
| 외생 (4 데이터셋 × 3 arm) | **11/12** | **10/12** |
| 표준 (4 데이터셋 × 3 arm) | **0/12** | **0/12** |

두 예외는 전부 ridge dynreg(gefcom_load의 기온-부하 비선형). 표준군에선 어떤 공변량 조건화
모델도 이기지 못한다. **이건 방법이 아니라 전략에 관한 결과다.**

### 2.4 경계의 위치 (분산 귀속) — 단단함, 정직한 판독은 더 강함

log-MSE 분산에서 norm×dataset 상호작용 **47.12%** vs 전 백본 항 0.6%. **단 이 47%는 외생
(CondNorm) arm이 만든다** — 내생 4 arm만 보면 상호작용 0.43%. 이건 김빠지는 게 아니라 **경계의
위치를 특정**한다: 내생족 안에서 정규화는 거의 무의미하고, 경계는 내생 대 외생 사이에 있다.

### 2.5 학습 전 부호 예측 8/8 (LPS) — 단단함(부호만)

사전등록(커밋 `cab17c1`, 첫 grid run **4분 13초 전**) 부호 예측이 8개 전부 적중. **MSE·MAE·
MASE 셋 다 8/8** — 지표 아티팩트가 아니다.

| | LPS | | | LPS | |
|---|---|---|---|---|---|
| gefcom_load | 0.894 | CN | electricity | 0.283 | IN |
| gefcom_solar | 0.875 | CN | weather | 0.110 | IN |
| jeju_wind | 0.745 | CN | etth2 | −0.205 | IN |
| gefcom_wind | 0.744 | CN | etth1 | −0.717 | IN |

### 2.6 합성·SOTA — 단단한 보조

- **GATE 1:** 2,640 run, λ*_실측 vs 이론 최대편차 0.015, horizon 확대 확인.
- **SOTA:** TimeXer/iTransformer에 arm 주입해도 RevIN−CondNorm 평균 +0.41 (CN 우세). 어댑터
  아티팩트 아님.

---

## 3. 무엇이 단단하지 않나 — 심사자가 여기서 논문을 끝낸다

### 3.1 LPS는 크기를 정렬하지 못한다 (분류기지 다이얼 아님) — **거짓 주장 철회**

전체 23셀 Spearman ρ=+0.783은 **두 클러스터 분리** 아티팩트다. **외생군 11셀 내부에서는
ρ=−0.750** (p=0.008) — LPS 최고 두 데이터셋(load 0.894, solar 0.875)이 격차 최소. 사전등록
보조 예측 "격차가 LPS에 단조 증가"는 **지지되지 않아 철회**. 중첩 셀 p=9.9e-6은 무효(클러스터
순열 p=0.023).

### 3.2 8/8의 집계 유의성은 과장 — 강등

2⁻⁸=0.004는 오해. 8개가 2개 상관 레짐이라 유효 n≈2, 정직한 p≈0.25. 정보는 데이터셋별이지
집계 적중 수가 아니다.

### 3.3 Shrinkage 블록은 판정을 못 했다 — 단, 사이징에 진단이 필요함

"축소하면 진단 불필요" 반론에 대한 α-응답(매칭 셀):

| 데이터셋 | LPS | α=0(Raw) | α=clip(LPS) | α=α̂ | α=1(CN) |
|---|---|---|---|---|---|
| etth1 | −0.72 | **0.386** | 0.387 | 0.511 | 2.751 |
| etth2 | −0.21 | **0.445** | 0.451 | 0.778 | 8.781 |
| weather | 0.11 | 0.185 | **0.177** | 0.190 | 0.842 |
| electricity | 0.28 | 0.144 | **0.139** | 0.155 | 0.173 |
| gefcom_wind | 0.74 | 1.072 | 0.208 | **0.169** | **0.169** |
| jeju_wind | 0.75 | 0.688 | 0.316 | **0.295** | **0.295** |

손실 최소 α가 LPS를 따라간다 → **α를 얼마나 축소할지 알려면 LPS가 필요**. 진단 없는 표준
재보정(α̂)은 표준군 4개 전부에서 Raw보다 나쁘다(엉뚱한 추정 대상: 예측 손실이 아니라 레벨 MSE
최소화). 파국 프레임은 부분 철회(Tier 1 발동), 하지만 반론이 진단을 제거하지 못하고 **재배치**할
뿐임을 보였다.

### 3.4 확률 구간 과소커버 — 정직한 실패

CondNorm은 외생군 **pinball 최소**(점예측 경계가 전이)지만, 실측 80% 커버리지가 외생군에서
0.50–0.66(jeju 0.576, gefcom_wind 0.502) vs RevIN 0.79–0.89. **"예비력 마진 축소" 서사와
충돌**(예비력=분위수 결정). 1단계 불확실성 미전파. 결과가 아니라 caveat로 유지.

---

## 4. 알려진 결함 — 외생 수치를 믿기 전에 읽을 것

1. **리드 매칭 주장 vs 구현 (결함).** 코드가 horizon별 밴드 선택을 안 한다
   (`g4_grid.py:461`이 horizon 루프 밖에서 1단계 1회 적합).
   - **jeju h=48**이 "h≤24-valid"라고 코드가 스스로 부르는 `ws_da`를 소비 → h=48 셀(유일한
     실데이터 Prop 3 검증)이 오염. 수리: `ws_d2`만으로 ~100셀 재실행.
   - **gefcom_load** 공변량이 실측 기온(예보 아님). 최고 LPS 데이터셋.
   - GEFCom wind/solar h=96/336은 **무해**(대회가 대상 월 전체 예보 블록 제공).
2. **단일 평가 원점.** rolling-origin 없음. 시드는 초기화만 바꾼다.
3. **지표 편중.** 주 결과가 z-scale MSE(MASE 추가함).
4. **LPS 이봉성.** 경계가 n=1(electricity)에서만 검증. graded-LPS 필요.
5. **고전 baseline 미실행.** ridge dynreg 대역이 CondNorm을 이김. 제대로 된 ARIMAX 미실행.
6. **Jeju 자가 큐레이션**(development 데이터셋으로 공개).

---

## 5. 진단 — 지금 이 재료는 어디까지 가나

**객관적 판정: 중위 Q1 게재 가능한 재료. 최상위 Q1은 아직 아님.** 글쓰기는 고쳤고, 천장은 이제
재료가 정한다. 천장을 누르는 셋(전부 재료의 속성):

1. **신규성 증분적** — `noise2025signal`(2510.04667)이 이미 학습 전 임계값 정규화 선택기를 냄
   (단 내생·손수정·실패). 남는 novelty(외생 통계·이론 앵커·사전등록)는 실재하나 좁다.
2. **제안 방법이 지배당함** — CondNorm이 Raw+cov·dynreg에 짐.
3. **진단이 분류기지 다이얼 아님** (§3.1).

| 목표 | 지금 재료 | +실험 하나·엄밀성 후 |
|---|---|---|
| IJF / Applied Energy 최상위 | 3–8% | 30–40% |
| Energy and AI, Neurocomputing, KBS, EAAI (중위 Q1) | 35–45% | **55–70%** |
| TMLR (품질 착지, 단 JCR quartile 없음) | 40–60% | 80–85% |
| **CCAI 워크샵 (비아카이브)** | **45–50%** | **70–75%** |

**천장을 실제로 올릴 유일한 지렛대:** graded-LPS 실험 — GEFCom 10 풍력존/3 태양광 분해(디스크에
있음, RLinear+LGBM, GPU 몇 시간). LPS를 분류기→다이얼로 승격 가능. 잘 나오면 최상위 Q1 확률
상승, 안 나와도 정직한 보고로 중위 Q1 유지.

**전략:** CCAI 워크샵을 값싸게 먼저 확보(임팩트 경로 정직화 + craft 수정, 글쓰기 며칠) →
graded-LPS로 저널 판본 확장. 워크샵은 저널 경로를 태우지 않는다.
